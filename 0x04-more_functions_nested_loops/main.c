#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	long r;
	int s;
	r = 612852475143;
	while (r % 2 == 0)
	       r /= 2;
	for (s = 3; s < r 
    return (0);
}
