#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_arr ( int *a , int n )
{
	int i  ; 
	for ( i =0 ; i< n ; i++)
		printf("%d, ", a[i]);
	putchar('\n');
}
int main(void)
{
char a[] = "look it up!";
string_toupper(a);
printf("%s", a);
    
    return (0);
}
