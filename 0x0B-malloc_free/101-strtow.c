#include"main.h"
#include<stdlib.h>
#include<string.h>
/**
 * strtow - function to create an array
 * @str: size of the array
 * Return: pointeer
*/
char **strtow(char *str)
{
	char **ptr;
	int len = 0, sprerator = 0;
	char *tmp = str;

	while (*tmp != '\0')
	{
		if
	}

}
